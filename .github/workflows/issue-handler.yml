name: Issue Handler

on:
  issues:
    types: [opened, edited]
  issue_comment:
    types: [created]
  discussion:
    types: [created]

concurrency:
  group: issue-handler-${{ github.event.issue.number || github.event.discussion.number || github.run_id }}
  cancel-in-progress: false

permissions:
  issues: write
  contents: read
  discussions: read
  models: read

jobs:
  triage:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    # Skip PR comments (handled by pr-review.yml) and bot's own comments (prevent loops)
    if: >-
      (github.event_name != 'issue_comment' || !github.event.issue.pull_request)
      && (github.event_name != 'issue_comment' || github.event.comment.user.login != 'scheilch')

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: pip install -r scripts/issue_handler/requirements.txt

      - name: Restore cost tracker cache
        uses: actions/cache/restore@v5
        with:
          path: /tmp/ai-cost-tracker
          key: ai-cost-tracker-${{ github.run_id }}
          restore-keys: |
            ai-cost-tracker-

      - name: Run issue handler
        id: handler
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BOT_PAT: ${{ secrets.BOT_PAT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_EVENT_TYPE: ${{ github.event_name }}
        run: python scripts/issue_handler/main.py

      - name: Save cost tracker cache
        if: always()
        uses: actions/cache/save@v5
        with:
          path: /tmp/ai-cost-tracker
          key: ai-cost-tracker-${{ github.run_id }}
